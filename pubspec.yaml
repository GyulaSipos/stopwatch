name: stopwatch
description: "Enterprise level stopwatch app"
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  # functional dependencies
  flutter_riverpod: ^3.3.1 #DI container and state management
  riverpod_lint: ^3.1.3 #linter extension to help development with riverpod
  sqflite: ^2.4.2+1 #to store state locally
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
