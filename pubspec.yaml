name: stopwatch
description: "Enterprise level stopwatch app"
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  # functional dependencies
  sqflite: ^2.4.2+1 #to store state locally

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
